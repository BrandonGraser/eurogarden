{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "git add -A; $staged = git diff --cached --quiet; if (-not $?) { git commit -m 'auto: save changes'; git push } 2>$null",
            "statusMessage": "Pushing to git..."
          }
        ]
      }
    ]
  }
}
